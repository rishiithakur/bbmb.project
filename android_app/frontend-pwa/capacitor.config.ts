import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bbmc.dwlms',
  appName: 'BBMC DWLMS',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
